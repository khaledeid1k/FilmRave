<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.FilmRave" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/denim</item>
        <item name="colorPrimaryDark">@color/ateneo_blue</item>
        <item name="colorAccent">@color/dark_bg</item>
        <item name="android:statusBarColor">@color/dark_bg</item>
    </style>

    <style name="Theme.FilmRave" parent="Base.Theme.FilmRave" />

    <style name="SheetDialog" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:colorBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.8</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="bottomSheetStyle">@style/bottomSheetStyleWrapper</item>
    </style>

    <style name="bottomSheetStyleWrapper" parent="Widget.Design.BottomSheet.Modal">
        <item name="behavior_peekHeight">550dp</item>
    </style>
</resources>